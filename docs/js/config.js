// Únic fitxer a editar després de desplegar Code.gs (veure DEPLOY.md).
const CONFIG = {
  // URL "/exec" que Google et dona en fer Deploy > New deployment > Web app.
  EXEC_URL: 'PENDENT_DE_DESPLEGAR',
  // Client ID d'OAuth creat a Google Cloud Console per al botó "Sign in with Google".
  GOOGLE_CLIENT_ID: 'PENDENT_DE_CONFIGURAR'
};
